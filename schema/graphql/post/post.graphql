type Post {
  id: ID!
  title: String!
  body: String!
  published: Boolean!
  author: User!
  comments: [Comment!]
}

type Mutation {
  addPost(
    title: String!
    body: String!
    published: Boolean
    authorEmail: String!
  ): Post
}
